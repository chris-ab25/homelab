apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: nginx-route
  namespace: test-nginx
spec:
  parentRefs:
    - name: main-gateway
      namespace: gateway-system
  hostnames:
    - "nginx.chrisab25.duckdns.org"
  rules:
    - backendRefs:
      - name: test-nginx
        port: 80