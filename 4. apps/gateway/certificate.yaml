apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-duckdns-cert
  namespace: envoy-gateway-system
spec:
  secretName: duckdns-tls-secret
  issuerRef:
    name: duckdns-letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - "*.chrisab25.duckdns.org"